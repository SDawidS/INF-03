kw1:

INSERT INTO odpowiedzi VALUES(NULL,1,5,"Przyda Ci się także wysoki stół, 
na którym postawisz pieska.");


kw2:

SELECT nick, (YEAR(CURRENT_DATE()) - rejestracja) AS latNaForum FROM konta WHERE nick IN("Jacek", "Tomek", "Monika");

kw3:

SELECT nick, postow, pytanie FROM konta INNER JOIN pytania ON pytania.konta_id = konta.id WHERE pytania.id = 1;

kw4:

SELECT odpowiedzi.id, odpowiedz, nick FROM odpowiedzi INNER JOIN konta ON konta.id = odpowiedzi.konta_id WHERE Pytania_id=1;