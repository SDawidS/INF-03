kw1:
SELECT id, imie, nazwisko, rok_urodzenia, zdjecie FROM osoby LIMIT 30;

kw2:
SELECT imie, nazwisko, rok_urodzenia, opis, zdjecie, nazwa FROM osoby INNER JOIN hobby ON hobby.id=osoby.hobby_id WHERE osoby.id=10;

kw3:
SELECT AVG(rok_urodzenia), MIN(rok_urodzenia), MAX(rok_urodzenia) FROM osoby;

kw4:
UPDATE osoby SET opis="jazda samochodem" WHERE Hobby_id=4;